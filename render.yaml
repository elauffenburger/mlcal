services:
  - type: web
    name: mlcal-api
    env: docker
    repo: https://github.com/elauffenburger/mlcal
    plan: free
    branch: main
    numInstances: 1
    healthCheckPath: /calendar?text
    envVars:
      - key: REDIS_HOST
        fromRedis:
          name: mlcal-api-redis
          property: host
      - key: REDIS_PORT
        fromRedis:
          name: mlcal-api-redis
          property: port
      - fromGroup: mlcal-api
redises:
  - name: mlcal-api-redis
    plan: ephemeral